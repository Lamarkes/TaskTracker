[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "todo-cli"
version = "0.1.0"
description = "Simple Todo CLI"
authors = [{name = "Lamark"}]

[project.scripts]
todo = "todo.cli:main"